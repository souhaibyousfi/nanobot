services:
  - type: web
    name: nanobot-service
    runtime: docker
    plan: free
    # التعديل الأساسي هنا:
    startCommand: "nanobot serve --host 0.0.0.0 --port 18790"
    envVars:
      - key: PORT
        value: "18790"
      - key: NODE_OPTIONS
        value: "--max-old-space-size=300"
